name: CI Test
on:
  push:
    branches: [ fix-issue-2 ]
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "CI pipeline working!"
      - name: Checkout
        uses: actions/checkout@v4
      - name: List files
        run: ls -la
